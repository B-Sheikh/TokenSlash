# Local frontend env — copy to .env.local
# Mock mode (default): leave VITE_API_BASE_URL unset, or set VITE_USE_MOCK=true
VITE_USE_MOCK=true

# After Checkpoint 2, point at the NitroStack API and disable mock:
# VITE_API_BASE_URL=http://localhost:3000
# VITE_USE_MOCK=false
